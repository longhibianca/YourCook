<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.iff.pooa20181.yourcook">

    <application
        android:name=".model.BDConfig"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListagemRefeicoesActivity"
            android:label="@string/title_activity_listagem_refeicoes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginUsuarioActivity" />
        <activity android:name=".CadastrarUsuarioActivity" />
        <activity android:name=".FiltroActivity" />
        <activity android:name=".CadastrarCatIngredienteActivity" />
        <activity android:name=".CadastrarIngredienteActivity"></activity>
    </application>

</manifest>